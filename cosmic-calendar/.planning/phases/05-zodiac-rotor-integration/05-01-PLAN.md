---
phase: 05-zodiac-rotor-integration
plan: 01
type: execute
wave: 1
depends_on:
  - 04-upcoming-qa
files_modified:
  - src/App.tsx
  - src/styles.css
  - flow-state.test.mjs
autonomous: false
requirements:
  - MOT-01
  - MOT-02
handoff_note: "Pending plan created during closeout. Do not execute until the current dirty handoff tree is committed and the integration decision is approved."
must_haves:
  truths:
    - "The current handoff implementation is preserved before any branch or worktree operation."
    - "The team explicitly chooses selective rotor integration or formal deferral; no whole-worktree merge occurs by accident."
    - "If integrated, one persistent zodiac renderer supports pointer and keyboard interaction without competing with the twelve-month gallery."
    - "If integrated, reduced-motion users receive an equivalent stable state and both app routes retain their verified section order."
---

<objective>
Resolve the isolated zodiac rotor deliberately: either port its useful behavior
behind the existing zodiac visual boundary and verify it, or move MOT-01/MOT-02
to a later milestone and close v1.0 without it.
</objective>

<context>
Reference implementation: branch `codex/rotor-prototype`, tip `84a8963`, retained
worktree `.worktrees/cosmiccal-rotor-prototype`. The main handoff branch contains
the accepted Phase 03.1/4 source. Confirm it is clean and matches the PR head
before inspecting, cherry-picking, rebasing, or switching branches.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Choose integration or formal deferral</name>
  <decision>Should the rotor replace the current zodiac renderer in this milestone?</decision>
  <context>Integration may add tactile exploration but must not introduce a second month-navigation model, regress the settled mobile composition, or overwrite the accepted client-feedback working tree.</context>
  <options>
    <option id="integrate">Selectively port only the renderer and interaction behavior needed behind the existing zodiac boundary; retain current flow, data, gallery, and upcoming architecture.</option>
    <option id="defer">Move MOT-01/MOT-02 to a future milestone, update requirements/roadmap/audit, and archive v1.0 without rotor claims.</option>
  </options>
  <resume-signal>Record `integrate` or `defer` in `.planning/DECISIONS.md` before continuing.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Execute the approved boundary</name>
  <files>src/App.tsx, src/styles.css, flow-state.test.mjs</files>
  <action>If `integrate`, inspect the isolated worktree read-only, port the minimum renderer/interaction surface, and adapt it to the existing app rather than merging the worktree wholesale. Preserve gallery semantics, portrait section sizing, keyboard operation, and the current data model. If `defer`, make no runtime change; move MOT-01/MOT-02 and update the GSD milestone records explicitly.</action>
  <verify><automated>npm run test:flow &amp;&amp; npm run build</automated></verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mobile interaction and close the milestone</name>
  <what-built>The approved rotor integration, or the explicitly documented no-rotor v1.0 boundary.</what-built>
  <how-to-verify>At portrait-mobile width, run birthday and skip routes; check pointer, swipe, keyboard, focus, reduced motion, section height, and horizontal overflow. If integrated, confirm the rotor communicates the same selected month as the gallery and never creates a competing selection state.</how-to-verify>
  <resume-signal>Approve the visual/device result, then create `05-01-SUMMARY.md`, `05-VERIFICATION.md`, rerun the milestone audit, and only then archive/tag v1.0.</resume-signal>
</task>

</tasks>

<success_criteria>
1. The published handoff branch is clean before rotor operations begin.
2. The integration/deferral decision is explicit and reflected in GSD state.
3. Any integrated renderer has pointer, keyboard, and reduced-motion parity.
4. Both primary routes, the month gallery, tests, and production build still pass.
5. Milestone completion claims match the final MOT-01/MOT-02 disposition.
</success_criteria>

<output>
If executed, create `05-01-SUMMARY.md`, `05-VERIFICATION.md`, and `05-UAT.md`,
then rerun the v1.0 milestone audit before completing or tagging the milestone.
</output>
