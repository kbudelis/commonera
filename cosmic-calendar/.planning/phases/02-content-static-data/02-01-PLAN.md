---
phase: 02-content-static-data
plan: 01
type: execute
wave: 1
depends_on:
  - 01-mobile-flow-skeleton
files_modified:
  - package.json
  - package-lock.json
  - src/content.ts
  - src/App.tsx
  - src/styles.css
  - flow-state.test.mjs
autonomous: true
requirements:
  - CAL-01
  - READ-01
  - READ-02
  - PROF-01
  - PROF-02
  - DATA-01
  - COPY-01
  - DATA-02
closeout_note: "Retroactive GSD record for inline implementation committed as e9d9677. Later client-feedback work changed presentation without replacing the typed date/content/storage foundation."
must_haves:
  truths:
    - "A civil date deterministically resolves to normalized Hebrew month facts, including Adar I/II normalization without losing the exact display label."
    - "The app bundles authored content for all twelve normalized Hebrew months and returns results without a server or runtime model."
    - "Birthday input produces a versioned local profile whose stable input can be rehydrated while authored prose remains replaceable."
    - "The primary experience remains reflective and non-predictive, with Jewish mystical grounding available as discovery rather than a prerequisite."
---

<objective>
Replace Phase 1 placeholders with a deterministic client-side Hebrew-calendar,
content, and persistence layer.

This plan was reconstructed during handoff from the implementation commit,
current source, requirements, and regression tests because Phase 2 was executed
inline without a canonical phase directory.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Implement and verify the static calendar/content foundation</name>
  <files>package.json, package-lock.json, src/content.ts, src/App.tsx, src/styles.css, flow-state.test.mjs</files>
  <action>Add `@hebcal/core`; define typed month correspondences and authored readings for all twelve normalized months; parse date-only birthday input at local noon; preserve exact Hebrew month labels while normalizing Adar I/II; derive a symbolic moon state from Hebrew day-of-month; persist only versioned input and stable derived facts in `localStorage`; render current-month, grounding, and personal-reading content through the existing Phase 1 flow. Keep all results deterministic and client-only.</action>
  <verify>
    <automated>npm run test:flow &amp;&amp; npm run build</automated>
  </verify>
  <done>The typed calendar/content/storage foundation exists, all normalized months resolve, invalid input fails safely, and the static production build passes.</done>
</task>

</tasks>

<success_criteria>
1. Current and birthday dates resolve through one typed Hebrew-calendar path.
2. Twelve normalized month entries contain a reading and concrete ritual.
3. Saved profiles contain versioned input/facts, not mutable authored prose.
4. Results are immediate, deterministic, non-predictive, and server-free.
</success_criteria>

<output>
Create `02-01-SUMMARY.md` and `02-VERIFICATION.md` when complete.
</output>
