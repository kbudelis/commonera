/*
  Working title. Naming is in active review after the 2026-07-11 client call
  (Rachel signaled a more universal register, e.g. "Rest Ritual") — keep every
  React-rendered product-name string flowing through this constant so a rename
  is a one-line change. Static surfaces that also carry the name when it
  changes: index.html <title>, manifest.webmanifest, and copy.md lines that
  embed the name.
*/
export const PRODUCT_NAME = 'Keep';
